diff --git a/src/app.ts b/src/app.ts
index 095ccda..bddd0b4 100644
--- a/src/app.ts
+++ b/src/app.ts
@@ -23,9 +23,10 @@ import type { ConfidenceLevel, ProgressMap, Question } from "./types";
 const STORAGE_KEY = "examforge:progress:v1";
 const OPTION_LETTERS = ["A", "B", "C", "D"] as const;
 
-// One confident accent color per topic. Assigned round-robin over the
-// sorted topic list for even spread across the fixed six-color palette.
-const TOPIC_PALETTE = ["#FF6B4A", "#2F5EFF", "#2FAE66", "#F2A93B", "#E4568C", "#1FA6A0"] as const;
+// One accent color per topic, pulled from a curated set of muted jewel
+// tones — same saturation/lightness family so they read as a considered
+// palette when scanning multiple cards, not a rainbow of unrelated hues.
+const TOPIC_PALETTE = ["#B65E2B", "#966D23", "#1D6F6B", "#3F5B7A", "#6B4C93", "#3F6B4A"] as const;
 
 function buildTopicColorMap(questions: Question[]): Map<string, string> {
   const topics = Array.from(new Set(questions.map((q) => q.topic))).sort();
diff --git a/src/styles.css b/src/styles.css
index 1a32a46..b563423 100644
--- a/src/styles.css
+++ b/src/styles.css
@@ -1,9 +1,11 @@
 /* ==========================================================================
    ExamForge — design tokens
-   Inspired by confident DTC/product landing pages: one bold accent color
-   owned per element (here, per topic — six accents cycle across topics),
-   big grotesk headlines, solid pill CTAs, and light/dark color-blocked
-   sections rather than one soft hue washed over everything.
+   One considered accent family, not a rainbow: a deep wine/burgundy
+   brand color, a warm near-black for dark panels (same temperature as
+   the brand accent, not an unrelated purple), and topic colors pulled
+   from a curated set of muted jewel tones — same saturation/lightness
+   range so a row of different topics still reads as one coherent
+   palette rather than default chart colors.
 
    Display: Space Grotesk (headlines, hero numerals, question text)
    Body:    Inter (UI chrome, prose)
@@ -11,29 +13,32 @@
    ========================================================================== */
 
 :root {
-  --surface: #faf7f2;
+  --surface: #faf8f4;
   --surface-raised: #ffffff;
-  --surface-sunken: #f2ede4;
-  --border-subtle: #e6ded0;
+  --surface-sunken: #f1ede6;
+  --border-subtle: #e4ddd1;
 
-  --ink: #171310;
-  --ink-muted: #6b6459;
+  --ink: #211c18;
+  --ink-muted: #746c60;
 
-  --ink-dark: #1b1533; /* the "dark band" tone — hero + shortcut panels */
-  --ink-dark-text: #f2eefa;
-  --ink-dark-text-muted: #b7aed1;
+  --ink-dark: #211417; /* warm near-black, same family as the wine accent */
+  --ink-dark-text: #f5efe9;
+  --ink-dark-text-muted: #c2a8ac;
 
-  --gain: #1f9d5c;
-  --gain-soft: #e1f5ea;
+  --gain: #2f7a4f;
+  --gain-soft: #e4f0e7;
 
-  --loss: #e2412f;
-  --loss-soft: #fce6e3;
+  --loss: #b23a2e;
+  --loss-soft: #f6e4e1;
+
+  --brand-accent: #8b2942;
+  --brand-accent-soft: #f3e1e6;
 
   --radius-card: 1.25rem;
   --radius-control: 0.65rem;
 
-  --shadow-sm: 0 1px 3px rgba(23, 19, 16, 0.06);
-  --shadow-md: 0 20px 40px rgba(23, 19, 16, 0.12);
+  --shadow-sm: 0 1px 3px rgba(33, 20, 23, 0.06);
+  --shadow-md: 0 20px 40px rgba(33, 20, 23, 0.12);
 
   --font-display: "Space Grotesk", ui-sans-serif, system-ui, sans-serif;
   --font-body: "Inter", ui-sans-serif, system-ui, sans-serif;
@@ -85,11 +90,6 @@ body {
    child is the router-driven #view-root.
    ========================================================================== */
 
-:root {
-  --brand-accent: #ff6b4a;
-  --brand-accent-soft: #ffe7de;
-}
-
 .app-shell {
   display: flex;
   min-height: 100vh;
@@ -171,7 +171,7 @@ body {
 
 .nav-link.is-active {
   background: var(--brand-accent-soft);
-  color: #c8451f;
+  color: #6b1f33;
 }
 
 .sidebar__divider {
@@ -296,8 +296,8 @@ body {
   width: fit-content;
   padding: 0.3rem 0.75rem;
   border-radius: 999px;
-  background: rgba(47, 174, 102, 0.18);
-  color: #6fe3a4;
+  background: rgba(63, 122, 79, 0.25);
+  color: #8fd4a8;
   font-size: 0.8rem;
   font-weight: 600;
 }
@@ -1007,7 +1007,7 @@ body {
   border: 1px solid var(--ink-dark);
 }
 .shortcut-solution .solution__title {
-  color: var(--topic-color, #ff6b4a);
+  color: var(--topic-color, var(--brand-accent));
 }
 .shortcut-solution .solution__body p {
   color: var(--ink-dark-text-muted);
